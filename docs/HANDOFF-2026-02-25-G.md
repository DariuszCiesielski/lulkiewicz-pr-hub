# Handoff — 2026-02-25-G — Usunięcie mocków + polskie znaki

## Co zrobione

| Zmiana | Status |
|--------|--------|
| Nowy endpoint `GET /api/fb-dashboard` — agregacja KPI z Supabase | Gotowe |
| Dashboard FB — prawdziwe dane (6 KPI tiles, developer summaries, relative time) | Gotowe |
| Reports FB — empty state (disabled button, info o przyszłej funkcji) | Gotowe |
| Analyze — domyślny prompt `FB_DEFAULT_PROMPT` zamiast placeholdera | Gotowe |
| Polskie diakrytyki — 16 plików, ~25 unikalnych stringów poprawionych | Gotowe |
| Usunięcie `src/lib/mock/fb-mock-data.ts` (367 linii) | Gotowe |
| Build: PASS | Gotowe |
| Push do master → Vercel auto-deploy | Gotowe |

## Nowe pliki

- `src/lib/ai/fb-default-prompt.ts` — wyekstrahowany domyślny prompt FB
- `src/app/api/fb-dashboard/route.ts` — endpoint agregujący KPI + developer summaries

## Zmodyfikowane pliki (kluczowe)

- `src/app/(hub)/fb-analyzer/dashboard/page.tsx` — pełny rewrite na `fetch('/api/fb-dashboard')`
- `src/app/(hub)/fb-analyzer/reports/page.tsx` — empty state zamiast mocków
- `src/app/(hub)/fb-analyzer/analyze/page.tsx` — `FB_DEFAULT_PROMPT` + polskie znaki
- 16 plików API routes — polskie diakrytyki (uprawnień, nieprawidłowy, błąd, itp.)

## Usunięte pliki

- `src/lib/mock/fb-mock-data.ts` — zero importów, bezpieczne usunięcie

## Commity (7)

1. `refactor: extract FB default prompt to dedicated file`
2. `feat: add /api/fb-dashboard endpoint with real Supabase aggregation`
3. `feat: dashboard — real Supabase data instead of mocks`
4. `feat: reports page — empty state instead of mock data`
5. `fix: analyze page — Polish diacritics + show default prompt`
6. `fix: add Polish diacritics to all FB module error messages and comments`
7. `chore: remove fb-mock-data.ts — all FB pages use real Supabase data`

## Następne kroki

1. Faza 11-12 FB Analyzer (raporty FB, eksport)
2. Podmiana cookies w fb_settings na cookies klientki
3. Dodanie 57 grup z listy przez bulk upload
4. Uruchomienie scrapowania + analizy AI
