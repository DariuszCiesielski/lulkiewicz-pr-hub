---
phase: 07-fb-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/tools.ts
  - src/components/layout/Sidebar.tsx
  - src/app/(hub)/fb-analyzer/layout.tsx
  - src/app/(hub)/fb-analyzer/page.tsx
  - src/app/(hub)/fb-analyzer/dashboard/page.tsx
  - src/app/(hub)/fb-analyzer/groups/page.tsx
  - src/app/(hub)/fb-analyzer/posts/page.tsx
  - src/app/(hub)/fb-analyzer/analyze/page.tsx
  - src/app/(hub)/fb-analyzer/reports/page.tsx
  - src/app/(hub)/fb-analyzer/settings/page.tsx
autonomous: true

must_haves:
  truths:
    - "Hub grid wyswietla karte Analizator Grup FB jako aktywne narzedzie (klikalne, nie Coming Soon)"
    - "Klikniecie karty FB Analyzer na hub przenosi do /fb-analyzer/dashboard"
    - "Sidebar pokazuje sekcje FB Analyzer z 6 children gdy uzytkownik jest w /fb-analyzer/*"
    - "Kazda z 6 stron shell renderuje tytul i placeholder informujacy o nadchodzacej funkcjonalnosci"
  artifacts:
    - path: "src/config/tools.ts"
      provides: "fb-analyzer aktywny (active: true, comingSoon: false)"
      contains: "active: true"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Nawigacja FB Analyzer z 6 children"
      contains: "fb-analyzer"
    - path: "src/app/(hub)/fb-analyzer/layout.tsx"
      provides: "Layout wrapper dla modulu FB"
    - path: "src/app/(hub)/fb-analyzer/page.tsx"
      provides: "Redirect /fb-analyzer -> /fb-analyzer/dashboard"
      contains: "router.replace"
    - path: "src/app/(hub)/fb-analyzer/dashboard/page.tsx"
      provides: "Shell page z placeholderem"
    - path: "src/app/(hub)/fb-analyzer/groups/page.tsx"
      provides: "Shell page z placeholderem"
    - path: "src/app/(hub)/fb-analyzer/posts/page.tsx"
      provides: "Shell page z placeholderem"
    - path: "src/app/(hub)/fb-analyzer/analyze/page.tsx"
      provides: "Shell page z placeholderem"
    - path: "src/app/(hub)/fb-analyzer/reports/page.tsx"
      provides: "Shell page z placeholderem"
    - path: "src/app/(hub)/fb-analyzer/settings/page.tsx"
      provides: "Shell page z placeholderem"
  key_links:
    - from: "src/config/tools.ts"
      to: "src/components/dashboard/ToolCard.tsx"
      via: "ToolCard czyta active/comingSoon z tools config"
      pattern: "active: true.*comingSoon: false"
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/app/(hub)/fb-analyzer/*/page.tsx"
      via: "NAV_ITEMS children href -> Next.js pages"
      pattern: "href: '/fb-analyzer/"
    - from: "src/app/(hub)/fb-analyzer/page.tsx"
      to: "src/app/(hub)/fb-analyzer/dashboard/page.tsx"
      via: "router.replace redirect"
      pattern: "router.replace.*fb-analyzer/dashboard"
---

<objective>
Aktywacja FB Analyzer na hub grid, dodanie nawigacji w sidebar z 6 children oraz utworzenie layoutu i 6 stron shell z placeholderami.

Purpose: Uzytkownik moze nawigowac do modulu FB Analyzer, widziec go jako aktywne narzedzie i przechodzic miedzy podstronami. Shell pages stanowia szkielet dla przyszlych faz.
Output: Zmodyfikowane tools.ts, Sidebar.tsx + 8 nowych plikow (layout, redirect, 6 shell pages)
</objective>

<execution_context>
@C:\Users\dariu\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\dariu\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Wzorzec tools.ts — fb-analyzer entry juz istnieje, wystarczy zmienic:
```typescript
{
  id: 'fb-analyzer',
  active: false,      // -> true
  comingSoon: true,    // -> false
}
```

Wzorzec Sidebar.tsx — NAV_ITEMS z children:
```typescript
{
  href: '/email-analyzer',
  label: 'Analizator Email',
  icon: Mail,
  badge: 'Aktywny',
  children: [
    { href: '/email-analyzer/dashboard', label: 'Dashboard', icon: BarChart3, adminOnly: true },
    // ...
  ],
}
```

Wzorzec layout.tsx (email-analyzer):
```typescript
'use client';
export default function EmailAnalyzerLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
```

Wzorzec redirect page.tsx (email-analyzer):
```typescript
'use client';
import { useEffect } from 'react';
import { useRouter } from 'next/navigation';
export default function EmailAnalyzerPage() {
  const router = useRouter();
  useEffect(() => { router.replace('/email-analyzer/dashboard'); }, [router]);
  return <div>...</div>;
}
```

Lucide-react ikony do uzycia dla FB children:
- Dashboard: BarChart3
- Grupy: Users (lub UsersRound)
- Posty: MessageSquare
- Analiza: Brain
- Raporty: ClipboardList (lub FileText)
- Ustawienia: Cog (lub Settings)

CSS Variables: --text-primary, --text-secondary, --text-muted, --bg-primary, --bg-secondary, --border-primary, --accent-primary
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aktywacja fb-analyzer w tools.ts + nawigacja sidebar</name>
  <files>src/config/tools.ts, src/components/layout/Sidebar.tsx</files>
  <action>
**tools.ts:**
Zmien wpis fb-analyzer:
- `active: false` -> `active: true`
- `comingSoon: true` -> `comingSoon: false`

**Sidebar.tsx:**
Dodaj nowy wpis w NAV_ITEMS PRZED wpisem admin (przed `{ href: '/admin', ... }`):

```typescript
{
  href: '/fb-analyzer',
  label: 'Analizator Grup FB',
  icon: MessageSquare,
  badge: 'Aktywny',
  children: [
    { href: '/fb-analyzer/dashboard', label: 'Dashboard', icon: BarChart3, adminOnly: true },
    { href: '/fb-analyzer/groups', label: 'Grupy', icon: Users, adminOnly: true },
    { href: '/fb-analyzer/posts', label: 'Posty', icon: MessageSquare, adminOnly: true },
    { href: '/fb-analyzer/analyze', label: 'Analiza', icon: Brain, adminOnly: true },
    { href: '/fb-analyzer/reports', label: 'Raporty', icon: ClipboardList, adminOnly: true },
    { href: '/fb-analyzer/settings', label: 'Ustawienia', icon: Cog, adminOnly: true },
  ],
},
```

Upewnij sie ze ikona `Users` jest zaimportowana w linii importow z lucide-react (dodaj jesli brak). Sprawdz ze `MessageSquare` jest juz zaimportowany (jest — widoczny w obecnym imporcie). Dodaj `Users` do importu jesli brakuje.

WAZNE: Ikona glownego wpisu FB Analyzer w sidebar to `MessageSquare` (ta sama co w tools.ts icon: 'MessageSquare'). Ale child "Posty" tez uzywa MessageSquare — to OK, rozne konteksty.
  </action>
  <verify>
1. W tools.ts: fb-analyzer ma active: true i comingSoon: false
2. W Sidebar.tsx: NAV_ITEMS zawiera wpis /fb-analyzer z 6 children
3. Wszystkie potrzebne ikony sa zaimportowane z lucide-react
4. Build przechodzi: `npm run build` nie rzuca bledow
  </verify>
  <done>Hub grid wyswietla FB Analyzer jako aktywne narzedzie. Sidebar pokazuje nawigacje FB Analyzer z 6 children (Dashboard, Grupy, Posty, Analiza, Raporty, Ustawienia).</done>
</task>

<task type="auto">
  <name>Task 2: Layout FB + redirect + 6 stron shell</name>
  <files>
    src/app/(hub)/fb-analyzer/layout.tsx
    src/app/(hub)/fb-analyzer/page.tsx
    src/app/(hub)/fb-analyzer/dashboard/page.tsx
    src/app/(hub)/fb-analyzer/groups/page.tsx
    src/app/(hub)/fb-analyzer/posts/page.tsx
    src/app/(hub)/fb-analyzer/analyze/page.tsx
    src/app/(hub)/fb-analyzer/reports/page.tsx
    src/app/(hub)/fb-analyzer/settings/page.tsx
  </files>
  <action>
**layout.tsx** — identyczny wzorzec jak email-analyzer:
```typescript
'use client';
export default function FbAnalyzerLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
```

**page.tsx** — redirect do dashboard (wzorzec email-analyzer):
```typescript
'use client';
import { useEffect } from 'react';
import { useRouter } from 'next/navigation';

export default function FbAnalyzerPage() {
  const router = useRouter();
  useEffect(() => {
    router.replace('/fb-analyzer/dashboard');
  }, [router]);

  return (
    <div className="flex min-h-[50vh] items-center justify-center">
      <p style={{ color: 'var(--text-muted)' }}>Przekierowanie...</p>
    </div>
  );
}
```

**6 stron shell** — kazda z osobnym tytulem i ikona. Wzorzec:
```typescript
'use client';
import { NazwaIkony } from 'lucide-react';

export default function NazwaStronyPage() {
  return (
    <div className="space-y-6">
      <div className="flex items-center gap-3">
        <NazwaIkony className="h-8 w-8" style={{ color: 'var(--accent-primary)' }} />
        <h1 className="text-2xl font-bold" style={{ color: 'var(--text-primary)' }}>
          Tytul strony
        </h1>
      </div>
      <div
        className="rounded-xl border p-8 text-center"
        style={{
          backgroundColor: 'var(--bg-secondary)',
          borderColor: 'var(--border-primary)',
        }}
      >
        <p className="text-lg" style={{ color: 'var(--text-secondary)' }}>
          Opis co bedzie na tej stronie
        </p>
        <p className="mt-2 text-sm" style={{ color: 'var(--text-muted)' }}>
          Ta funkcjonalnosc zostanie zaimplementowana w kolejnych fazach.
        </p>
      </div>
    </div>
  );
}
```

Konkretne strony shell:

1. **dashboard/page.tsx**: ikona BarChart3, tytul "Dashboard FB", opis "Metryki i podsumowania z monitorowanych grup Facebookowych"
2. **groups/page.tsx**: ikona Users, tytul "Grupy FB", opis "Zarzadzanie monitorowanymi grupami Facebookowymi"
3. **posts/page.tsx**: ikona MessageSquare, tytul "Posty", opis "Przegladanie istotnych postow z grup Facebookowych"
4. **analyze/page.tsx**: ikona Brain, tytul "Analiza AI", opis "Analiza sentymentu i kategoryzacja postow z grup FB"
5. **reports/page.tsx**: ikona ClipboardList, tytul "Raporty FB", opis "Generowanie raportow z analizy grup Facebookowych"
6. **settings/page.tsx**: ikona Cog, tytul "Ustawienia FB", opis "Konfiguracja Apify, cookies Facebooka i parametrow scrapowania"

WAZNE: Wszystkie strony sa 'use client'. Kazda strona to osobny plik. Tytuly i opisy w jezyku polskim.
  </action>
  <verify>
1. Kazdy plik istnieje i jest poprawnym komponentem React
2. Build przechodzi bez bledow: `npm run build`
3. Nawigacja /fb-analyzer redirectuje do /fb-analyzer/dashboard
4. Kazda z 6 stron renderuje tytul i placeholder
  </verify>
  <done>Layout, redirect i 6 stron shell istnieja. Uzytkownik moze nawigowac do kazdej podstrony FB Analyzer i zobaczyc placeholder z informacja o nadchodzacej funkcjonalnosci.</done>
</task>

</tasks>

<verification>
1. Na stronie glownej hub — karta "Analizator Grup FB" jest aktywna i klikalna (nie Coming Soon)
2. Klikniecie karty przenosi do /fb-analyzer -> redirect do /fb-analyzer/dashboard
3. Sidebar pokazuje sekcje FB Analyzer z 6 children gdy pathname zaczyna sie od /fb-analyzer
4. Kazda z 6 stron (/dashboard, /groups, /posts, /analyze, /reports, /settings) renderuje tytul + placeholder
5. Build (`npm run build`) przechodzi bez bledow
</verification>

<success_criteria>
- FB Analyzer jest widoczny jako aktywne narzedzie na hub grid
- Nawigacja sidebar dziala z 6 children
- Wszystkie strony shell renderuja poprawnie z CSS Variables design system
- Zero bledow TypeScript, zero bledow build
</success_criteria>

<output>
After completion, create `.planning/phases/07-fb-foundation/07-02-SUMMARY.md`
</output>
